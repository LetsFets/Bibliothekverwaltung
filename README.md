# Bibliothekverwaltung

Projekt-Template für die Bibliothekverwaltung (React frontend + Node/Express backend mit MariaDB via Sequelize).

Siehe die Unterordner `server` und `client` für Startanweisungen.
